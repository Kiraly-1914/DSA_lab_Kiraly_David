#include <stdio.h>

int main() {

    int x;
    Digit* array;

    printf("number=");
    scanf("%i"; &number);

    do{
        printf("x=");
        scanf("%i"; &x);

        switch(x) {
            case 1:
                Print(array);
                break;
            case 2:
                printf("k=");
                scanf("%i"; &k);
                Digit_k(array, k);
                break;
            case 3:
                sizeDigit(array)
                break;
            case
        }
    }while(x!=0);








    return 0;
}
